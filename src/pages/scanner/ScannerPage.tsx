import { Box } from '@hope-ui/solid';

import { Button } from '../../components/Button';

export const ScannerPage = () => {
    return <Box>
        <Button>
            Scan area
        </Button>
    </Box>;
};
